{
  "name": "void-run",
  "version": "1.0.0",
  "description": "Negative Space ASCII Space Shooter Roguelike",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.7"
  }
}

